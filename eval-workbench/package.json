{
  "name": "eval-workbench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A hands-on workbench for building an LLM eval set: dataset, questions, rubric, runs, human labels, LLM judge, and judge-human agreement.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0"
  }
}
